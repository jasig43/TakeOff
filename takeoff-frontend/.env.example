# Copy to `.env.local` for local development. Never commit real `.env` files.

# Base URL of the TakeOFF backend REST API (must match the backend port).
VITE_API_BASE_URL=http://localhost:8080/api/v1
